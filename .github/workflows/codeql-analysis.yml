---

name: "CodeQL"

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '31 7 * * 3'

permissions:
  contents: read

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        language: [ 'TypeScript' ]

    permissions:
      actions: read
      security-events: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@7cdaf2fbc075e6f3b9ca94cfd6cec5adc8a75622

      - name: Initialize CodeQL
        uses: github/codeql-action/init@78bfd293013bf03240e1595bb7273da04cddfa75
        with:
          languages: ${{ matrix.language }}
          source-root: src

      - name: Autobuild
        uses: github/codeql-action/autobuild@78bfd293013bf03240e1595bb7273da04cddfa75

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@78bfd293013bf03240e1595bb7273da04cddfa75

...
